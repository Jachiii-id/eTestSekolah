List all existing API routes in `backend/routes/routes.js` and all frontend routes in `frontend/src/router/index.js`.

Then check: $ARGUMENTS

Show if there are any conflicts, missing auth guards, or routes that don't have a corresponding controller function.
