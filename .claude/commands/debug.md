Debug this issue in the Markas Surabaya project:

$ARGUMENTS

Steps:
1. Read the relevant controller, model, and Vue component
2. Trace the data flow from frontend → axios → backend route → controller → model → DB
3. Identify the root cause
4. Fix it with minimal changes — don't refactor unrelated code
5. Use snake_case for any new code, UPPERCASE for DB column names
